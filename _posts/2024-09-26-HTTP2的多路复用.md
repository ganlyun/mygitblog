# HTTP/2的多路复用

### 解决什么问题？

要解决HTTP/1.1的队头阻塞问题



### 问题产生原因？

应用层需要按顺序发送数据，小体积数据会被大体积的阻塞



### 解决方法

* 应用层使用**流**管理并发通信，一个TCP连接可以有多个流，每个流独立发送和接收数据

* 传输层使用**帧**发送，每个帧包含对应流的标识，不同流的帧可以交错发送



### 没有解决什么？

传输层的队头阻塞并没有解决，一个帧丢失会阻塞后续帧的发送

